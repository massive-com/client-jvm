/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package com.massive.client.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param userCountry Country of consumer's residence (ISO codes). Available countries: UK, DE, FR, ES, IT, AT. Values outside these are mapped to 'unknown'.
 * @param channel Transaction channel. Possible values: online, offline, bnpl (buy now pay later platforms such as Scala, Klarna, Zilch).
 * @param consumerType The panel the account is sourced from. Possible values: consumer_credit, consumer_debit, open_banking.
 * @param eightDayRollingCategoryAccounts The count of unique consumer accounts active for a given country, consumer type, and that transacted in this merchant category (MCC Group) over the prior 8 days. Used for short-term normalization calculations (e.g., spend per 1000 panelists = (total_spend / eight_day_rolling_category_accounts) * 1000).
 * @param eightDayRollingTotalAccounts The count of unique consumer accounts active for a given country and consumer type that had any transaction activity (across all categories) over the prior 8 days. Provides a baseline for total panel size.
 * @param mccGroup Merchant category code group associated with the merchant or payment processor.
 * @param merchantIndustry Industry name based on GICS/BICS/ICB data classification for the merchant.
 * @param merchantTicker Stock ticker associated with the merchant (~250 US public companies mapped). Uses Bloomberg tickers and they are point in time.
 * @param name The merchant or entity name for this aggregate (lowercase). When type is 'merchant', this value can be used as the lookup_name in the merchant-hierarchy endpoint to retrieve full corporate hierarchy details.
 * @param parentName Merchant's parent business name (Title Case). Useful for aggregating transactions across subsidiary brands. Also available in the merchant-hierarchy endpoint for full corporate structure.
 * @param publishedDate The date when this data version was published. For original data, this is approximately 7 days after transaction_date.
 * @param spendInDistinctAccountKeyCount The count of distinct account keys (unique consumer accounts) with inbound transactions.
 * @param spendInSpend The total inbound transaction amount (refunds, returns, credits) in the specified currency for this aggregation. Values are positive, representing money flowing back into consumer accounts.
 * @param spendInTransactionCount The count of inbound transactions (refunds, returns).
 * @param spendOutDistinctAccountKeyCount The count of distinct account keys (unique consumer accounts) with outbound transactions.
 * @param spendOutSpend The total outbound transaction amount (money spent by consumers) in the specified currency for this aggregation. Values are negative, representing money flowing out of consumer accounts.
 * @param spendOutTransactionCount The count of outbound transactions (purchases, payments).
 * @param totalAccounts The total count of distinct consumer accounts with any transaction activity for this aggregation.
 * @param totalSpend Sum of spend_out_spend and spend_in_spend. Typically negative (net outflow). A positive value indicates refunds exceeded new spending for this aggregation.
 * @param totalTransactions The total count of all transactions (outbound + inbound).
 * @param transactionCurrency ISO currency code for the transaction (base card/account currency). Always EUR or GBP in this dataset. All spend amounts are denominated in this currency.
 * @param transactionDate The calendar date when the consumer transactions occurred.
 * @param twentyEightDayRollingCategoryAccounts The count of unique consumer accounts active for a given country, consumer type, and that transacted in this merchant category (MCC Group) over the prior 28 days. Used for longer-term normalization calculations.
 * @param twentyEightDayRollingTotalAccounts The count of unique consumer accounts active for a given country and consumer type that had any transaction activity (across all categories) over the prior 28 days. Provides a longer-term baseline for total panel size.
 * @param type The type of aggregation. Can be 'merchant' or 'payment_processor'.
 */


data class GetConsumerSpendingEuV1MerchantAggregates200ResponseResultsInner (

    /* Country of consumer's residence (ISO codes). Available countries: UK, DE, FR, ES, IT, AT. Values outside these are mapped to 'unknown'. */
    @Json(name = "user_country")
    val userCountry: kotlin.String,

    /* Transaction channel. Possible values: online, offline, bnpl (buy now pay later platforms such as Scala, Klarna, Zilch). */
    @Json(name = "channel")
    val channel: kotlin.String? = null,

    /* The panel the account is sourced from. Possible values: consumer_credit, consumer_debit, open_banking. */
    @Json(name = "consumer_type")
    val consumerType: kotlin.String? = null,

    /* The count of unique consumer accounts active for a given country, consumer type, and that transacted in this merchant category (MCC Group) over the prior 8 days. Used for short-term normalization calculations (e.g., spend per 1000 panelists = (total_spend / eight_day_rolling_category_accounts) * 1000). */
    @Json(name = "eight_day_rolling_category_accounts")
    val eightDayRollingCategoryAccounts: kotlin.Long? = null,

    /* The count of unique consumer accounts active for a given country and consumer type that had any transaction activity (across all categories) over the prior 8 days. Provides a baseline for total panel size. */
    @Json(name = "eight_day_rolling_total_accounts")
    val eightDayRollingTotalAccounts: kotlin.Long? = null,

    /* Merchant category code group associated with the merchant or payment processor. */
    @Json(name = "mcc_group")
    val mccGroup: kotlin.String? = null,

    /* Industry name based on GICS/BICS/ICB data classification for the merchant. */
    @Json(name = "merchant_industry")
    val merchantIndustry: kotlin.String? = null,

    /* Stock ticker associated with the merchant (~250 US public companies mapped). Uses Bloomberg tickers and they are point in time. */
    @Json(name = "merchant_ticker")
    val merchantTicker: kotlin.String? = null,

    /* The merchant or entity name for this aggregate (lowercase). When type is 'merchant', this value can be used as the lookup_name in the merchant-hierarchy endpoint to retrieve full corporate hierarchy details. */
    @Json(name = "name")
    val name: kotlin.String? = null,

    /* Merchant's parent business name (Title Case). Useful for aggregating transactions across subsidiary brands. Also available in the merchant-hierarchy endpoint for full corporate structure. */
    @Json(name = "parent_name")
    val parentName: kotlin.String? = null,

    /* The date when this data version was published. For original data, this is approximately 7 days after transaction_date. */
    @Json(name = "published_date")
    val publishedDate: java.time.LocalDate? = null,

    /* The count of distinct account keys (unique consumer accounts) with inbound transactions. */
    @Json(name = "spend_in_distinct_account_key_count")
    val spendInDistinctAccountKeyCount: kotlin.Long? = null,

    /* The total inbound transaction amount (refunds, returns, credits) in the specified currency for this aggregation. Values are positive, representing money flowing back into consumer accounts. */
    @Json(name = "spend_in_spend")
    val spendInSpend: kotlin.Double? = null,

    /* The count of inbound transactions (refunds, returns). */
    @Json(name = "spend_in_transaction_count")
    val spendInTransactionCount: kotlin.Long? = null,

    /* The count of distinct account keys (unique consumer accounts) with outbound transactions. */
    @Json(name = "spend_out_distinct_account_key_count")
    val spendOutDistinctAccountKeyCount: kotlin.Long? = null,

    /* The total outbound transaction amount (money spent by consumers) in the specified currency for this aggregation. Values are negative, representing money flowing out of consumer accounts. */
    @Json(name = "spend_out_spend")
    val spendOutSpend: kotlin.Double? = null,

    /* The count of outbound transactions (purchases, payments). */
    @Json(name = "spend_out_transaction_count")
    val spendOutTransactionCount: kotlin.Long? = null,

    /* The total count of distinct consumer accounts with any transaction activity for this aggregation. */
    @Json(name = "total_accounts")
    val totalAccounts: kotlin.Long? = null,

    /* Sum of spend_out_spend and spend_in_spend. Typically negative (net outflow). A positive value indicates refunds exceeded new spending for this aggregation. */
    @Json(name = "total_spend")
    val totalSpend: kotlin.Double? = null,

    /* The total count of all transactions (outbound + inbound). */
    @Json(name = "total_transactions")
    val totalTransactions: kotlin.Long? = null,

    /* ISO currency code for the transaction (base card/account currency). Always EUR or GBP in this dataset. All spend amounts are denominated in this currency. */
    @Json(name = "transaction_currency")
    val transactionCurrency: kotlin.String? = null,

    /* The calendar date when the consumer transactions occurred. */
    @Json(name = "transaction_date")
    val transactionDate: java.time.LocalDate? = null,

    /* The count of unique consumer accounts active for a given country, consumer type, and that transacted in this merchant category (MCC Group) over the prior 28 days. Used for longer-term normalization calculations. */
    @Json(name = "twenty_eight_day_rolling_category_accounts")
    val twentyEightDayRollingCategoryAccounts: kotlin.Long? = null,

    /* The count of unique consumer accounts active for a given country and consumer type that had any transaction activity (across all categories) over the prior 28 days. Provides a longer-term baseline for total panel size. */
    @Json(name = "twenty_eight_day_rolling_total_accounts")
    val twentyEightDayRollingTotalAccounts: kotlin.Long? = null,

    /* The type of aggregation. Can be 'merchant' or 'payment_processor'. */
    @Json(name = "type")
    val type: kotlin.String? = null

) {


}

